/*
import { Component, Input } from '@angular/core';
import { IConfiguration } from '../configuration';

@Component({
    selector: 'configuration-text-renderer',
    templateUrl: 'configuration-text-renderer.component.html',
    styles: [`
        .configuration-list {
            padding-left: 20px;
            list-style: none;
        }
    `]
})
export class ConfigurationTextRenderer {
    @Input() configuration: IConfiguration
}
*/