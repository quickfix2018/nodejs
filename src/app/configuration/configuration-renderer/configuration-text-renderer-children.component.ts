/*
import { Component, Input } from '@angular/core';
import { IConfiguration } from '../configuration';

@Component({
    selector: 'configuration-text-renderer-children',
    templateUrl: 'configuration-text-renderer-children.component.html',
    styles: [`
        .configuration-list {
            padding-left: 20px;
            list-style: none;
        }
    `]
})
export class ConfigurationTextRendererChildren {
    @Input() configuration: IConfiguration;
}
*/